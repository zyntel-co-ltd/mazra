/**
 * Legacy Kanta-target seeder removed. Mazra Hospital uses profile-scoped
 * loaders (see docs/MAZRA_HOSPITAL_PLAN.md).
 */
console.error(
  "npm run seed is disabled — Mazra Hospital dataset loaders are not wired yet."
);
process.exit(1);
