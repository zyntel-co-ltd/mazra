/**
 * Legacy mode JSON builder removed. Datasets are 12-month per profile (ENG-116+).
 */
console.error(
  "npm run build:dataset is disabled — use future @zyntel/mazra generators."
);
process.exit(1);
