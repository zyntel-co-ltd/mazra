{
  "name": "mazra-web",
  "type": "module",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "pages:deploy": "wrangler pages deploy dist --project-name=mazra-web --commit-dirty=true"
  },
  "dependencies": {
    "astro": "^5.7.0"
  },
  "devDependencies": {
    "wrangler": "^4.76.0"
  }
}
